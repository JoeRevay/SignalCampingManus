Scale traffic for my existing SignalCamping project by expanding high-intent, programmatic SEO pages that use my current campground dataset and existing affiliate recommendation system.

IMPORTANT:
- Do NOT redesign the site
- Do NOT add new libraries unless absolutely necessary
- Reuse existing components and data structures where possible
- Keep everything production-clean and safe
- Focus on pages that are most likely to rank and monetize
- Use my existing campground dataset and current routing structure
- Keep URLs clean and human-readable
- Do not modify unrelated files

GOAL:
Build a scalable traffic layer on top of my current site by creating new high-intent SEO landing pages that:
1. target carrier + state search intent
2. target remote-work + state search intent
3. target strong cell-service + state search intent
4. use my existing affiliate module near the top of the page
5. are indexable, internally linked, and included in the sitemap

==================================================
PART 1 — AUDIT CURRENT PROJECT STRUCTURE FIRST
==================================================

Before changing code:
- inspect the current routing and existing page components
- inspect my actual dataset field names in client/src/data/campgrounds.json
- inspect any existing state, carrier, list, or ranking pages already present
- inspect existing sitemap generation logic
- inspect how metadata is currently set on pages

Use the real existing field names and file structure.
Do NOT assume names if they already exist.

After the audit, implement the following using the actual project structure.

==================================================
PART 2 — CREATE ONE REUSABLE RANKING PAGE TEMPLATE
==================================================

Create a new reusable page component for SEO ranking pages.

Suggested file:
client/src/pages/SeoRankingPage.tsx

This page should support multiple ranking page types based on route params or config.

It must support these page families:

1. Carrier + state
Examples:
- /best-campgrounds-with-verizon-signal-in-ohio
- /best-campgrounds-with-att-signal-in-michigan
- /best-campgrounds-with-tmobile-signal-in-pennsylvania

2. Remote work + state
Examples:
- /best-remote-work-campgrounds-in-ohio
- /best-remote-work-campgrounds-in-michigan

3. Strong signal + state
Examples:
- /campgrounds-with-strong-cell-service-in-ohio
- /campgrounds-with-strong-cell-service-in-michigan

Requirements:
- use react-router-dom params
- load and filter client/src/data/campgrounds.json
- render ranked campground results
- include short intro copy relevant to the page type
- include affiliate recommendations near the top using the top-ranked campground as context
- include links to campground detail pages
- include reservation link / existing campground CTA if already supported
- handle empty results safely
- no crashes on missing values

==================================================
PART 3 — USE REAL DATA LOGIC
==================================================

Use the real field names from my dataset after inspecting them.

Expected logic pattern:
- state-based filtering by state or state_full
- carrier pages filter by carrier-specific coverage field
- remote-work pages sort/filter by remote_work_score
- strong-signal pages sort/filter by signal_quality_score and/or signal_score

Ranking logic:
- sort descending by the most relevant score for the page type
- use a sensible secondary sort if needed
- limit to a reasonable number like top 25

Do not fabricate fields.
Use exactly what exists in the project.

==================================================
PART 4 — PAGE CONTENT STRUCTURE
==================================================

Each new SEO page should include, in this order:

1. H1 title
2. short intro paragraph
3. affiliate recommendation module near the top using the top result as context
4. ranked list of campgrounds
5. useful supporting copy near the bottom
6. internal links to related pages if possible

Each ranked entry should show:
- rank number
- campground name
- city + state
- the most relevant score(s) for the page type
- carrier availability summary if available
- link to campground detail page

Keep styling simple and consistent with current site.

==================================================
PART 5 — SEO METADATA
==================================================

For each page, set:
- document.title
- meta description

Use page-type-specific copy.

Examples:
Carrier page:
"Best Campgrounds with Verizon Signal in Ohio (2026)"

Remote work page:
"Best Remote Work Campgrounds in Ohio (2026)"

Strong signal page:
"Campgrounds with Strong Cell Service in Ohio (2026)"

Meta descriptions should be readable, not spammy, and tied to the page type.

==================================================
PART 6 — ROUTES
==================================================

Add routes for these new page families using the reusable page template.

Support these patterns:

1. /best-campgrounds-with-:carrier-signal-in-:state
2. /best-remote-work-campgrounds-in-:state
3. /campgrounds-with-strong-cell-service-in-:state

Use the actual App.tsx routing structure already in the project.

==================================================
PART 7 — INTERNAL LINKING
==================================================

This is critical.

Add internal links so these pages can actually get discovered and ranked.

Requirements:
- from campground detail pages, add links to related state/carrier pages when relevant
- from state pages, add links to the new carrier and remote-work pages for that state
- from ranking pages, add links to related ranking pages for the same state
- do not overdo it; keep links useful and natural

Examples:
On an Ohio page, link to:
- Best Campgrounds with Verizon Signal in Ohio
- Best Remote Work Campgrounds in Ohio
- Campgrounds with Strong Cell Service in Ohio

==================================================
PART 8 — SITEMAP INTEGRATION
==================================================

Inspect the existing sitemap generation logic and add the new ranking pages.

Requirements:
- include all valid state/carrier pages supported by real data
- include all valid remote-work pages supported by real data
- include all valid strong-signal pages supported by real data
- do not add pages for empty result sets
- generate URLs from real available states in the data

Do not guess the states. Derive them from the real dataset.

==================================================
PART 9 — AFFILIATE MODULE REUSE
==================================================

Reuse the existing affiliate recommendation module already built.

Requirements:
- place it near the top of the new ranking pages
- use the first/top-ranked campground as the context campground
- only render if a valid top campground exists
- do not duplicate the module multiple times on one page

==================================================
PART 10 — MONETIZATION SUPPORT
==================================================

Wherever relevant and already supported by my project:
- show existing campground reservation / outbound CTA
- keep affiliate recommendation module intact
- do not introduce static product links

The new traffic pages should support monetization naturally, not look ad-heavy.

==================================================
PART 11 — SAFETY / QUALITY
==================================================

- handle missing values safely
- no runtime crashes
- no blank broken sections
- no unrelated refactors
- keep code readable and maintainable
- do not duplicate logic unnecessarily if helper functions should be reused

==================================================
PART 12 — AFTER IMPLEMENTATION
==================================================

After finishing:
1. summarize which files were created
2. summarize which files were modified
3. list the exact new route patterns added
4. report how many valid state pages were generated from the real dataset for:
   - carrier pages
   - remote-work pages
   - strong-signal pages
5. confirm sitemap integration was completed
6. confirm where internal links were added